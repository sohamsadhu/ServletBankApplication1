CREATE TABLE ADDRESS( ADD_ID NUMBER(3) PRIMARY KEY, APT_NO NUMBER(3), STREET VARCHAR2(40) NOT NULL,
CITY VARCHAR2(20) NOT NULL, STATE VARCHAR(2) NOT NULL, ZIP NUMBER(5) NOT NULL);
CREATE TABLE CUSTOMER( CUSTOMER_ID NUMBER(3) PRIMARY KEY, FIRST_NAME VARCHAR2(20), LAST_NAME VARCHAR2(20),
DOB DATE, ADD_ID NUMBER(3), BALANCE NUMBER(6), CONSTRAINT ADDRESS_FK FOREIGN KEY (ADD_ID) REFERENCES
ADDRESS(ADD_ID) ON DELETE CASCADE);
CREATE SEQUENCE ACCOUNT_NUMBER INCREMENT BY 1 START WITH 1 NOMAXVALUE NOCYCLE CACHE 10;
-- Dropping the table Customer and recreating it to get Account id in there.
-- A option is to copy contents of table from one to another temporary one, and then drop and copy it into
-- a new table of the same name as shown here http://stackoverflow.com/a/5392204
DROP TABLE CUSTOMER;
DROP TABLE ADDRESS;

CREATE TABLE ADDRESS( CUSTOMER_ID NUMBER(3) PRIMARY KEY, APT_NO NUMBER(3), STREET VARCHAR2(40) NOT NULL,
CITY VARCHAR2(20) NOT NULL, STATE VARCHAR(2) NOT NULL, ZIP NUMBER(5) NOT NULL);

CREATE TABLE CUSTOMER(ACCOUNT_NUMBER NUMBER(3) PRIMARY KEY, CUSTOMER_ID NUMBER(3),  FIRST_NAME VARCHAR2(20), 
LAST_NAME VARCHAR2(20), DOB DATE, ADD_ID NUMBER(3), BALANCE NUMBER(6), CONSTRAINT ADDRESS_FK FOREIGN KEY 
(CUSTOMER_ID) REFERENCES ADDRESS(CUSTOMER_ID) ON DELETE CASCADE);

ALTER TABLE CUSTOMER DROP COLUMN ADD_ID;
ALTER TABLE ADDRESS MODIFY STATE VARCHAR2(15);